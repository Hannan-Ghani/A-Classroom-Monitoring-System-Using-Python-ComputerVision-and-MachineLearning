val sourceData = transactionSource.select(alignedSourceCols.zipWithIndex.map { case (columnName, i) => col(columnName).as(s"col_$i") }: _*)
val targetData = caseClass.select(alignedTargetCols.zipWithIndex.map { case (columnName, i) => col(columnName).as(s"col_$i") }: _*)