# Path to the customer data (input)
customer_path=s3://my-data-bucket/customer/20240129/raw/parquet/*.parquet

# Path to the product data (input)
product_path=s3://my-data-bucket/product/parquet/

# Path to the target data (input)
target_path=s3://my-data-bucket/target/parquet/

# Path to store intermediate and output data
output_path=s3://my-output-bucket/output/

# Path to store the differences summary text file
output_file=s3://my-output-bucket/output/differences_summary.txt