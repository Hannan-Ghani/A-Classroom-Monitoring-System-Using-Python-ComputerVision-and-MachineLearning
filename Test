def writeSummaryLog(log: List[String], outputPath: String)(implicit spark: SparkSession): Unit = {
  import spark.implicits._
  val summaryDF = spark.sparkContext.parallelize(log).toDF("Summary")
  summaryDF.write.mode("overwrite").text(outputPath)
  println(s"Successfully wrote summary log to $outputPath")
}